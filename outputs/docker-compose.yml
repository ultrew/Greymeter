version: '3.8'
services:
  stego-app:
    build: .
    container_name: Greymeter
    ports:
      - "8501:8501"
    volumes:
      - ./outputs:/app/outputs       
      - ./sample_images:/app/sample_images